<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your Apps</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>My Expenses</h1>
    <ul>
        <% expense.forEach((item)=>{ %>
  
        <li>
            <a href="/users/<%= user._id %>/dashboard/my-expenses/<%=item._id%>">
            <%= item.name %> at <%= item.amount %>
            </a>
        </li>
        <% }) %>
      </ul>
      <button><a href="/users/<%=user._id%>/dashboard/new-expense">Add an Expense</a></button>
  </body>
</html>
